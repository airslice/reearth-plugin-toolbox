import _ from "lodash";

import { commonTheme } from "../common";

export default _.merge({}, commonTheme, {
  colors: {
    background: "#ECECEC",
    primary: "#F57C4B",
    secondary: "#FC5B00",
  },
});
